# Tokenization：大语言模型如何理解文字

大语言模型（LLM）本身不直接处理人类文字，而是先把文本切分成一个个 Token，再映射为向量参与计算。这个切分过程就叫 Tokenization。

## 什么是 Token

Token 是 LLM 处理文本的最小单位。它不一定是完整的单词：常见的词可能整个就是一个 Token，生僻词或长词通常会被拆成多个子词（subword）。例如 "unhappiness" 可能被拆为 "un", "happi", "ness" 三个 Token。

中文一般按字或字的组合切分，常用汉字组合可能被合并为一个 Token。

不同模型的分词器（Tokenizer）不同，所以同一文本在 GPT、Claude、豆包之间的 Token 数量往往不一样。

## 为什么不直接按字符或单词

按字符切会导致序列太长（一句话变几十个 Token），训练和推理成本飙升；按单词切则词汇表爆炸，未登录词（OOV）无法处理。

子词分词（Subword Tokenization）是目前的主流折中方案：常用词保留完整，罕见词拆成有意义的片段。这样词汇表大小可控（通常 3 万~10 万），又能推断组合新词。

## BPE 算法

Byte Pair Encoding（BPE）是最常用的子词分词算法。步骤：

1. 初始词汇表包含所有单个字符或字节。
2. 统计训练语料中最常相邻出现的两个单元，合并为一个新单元加入词汇表。
3. 重复第 2 步直到达到预设的词汇表大小。

推理时，对新文本做最长匹配拆分，优先匹配词汇表里更长的单元。

## Token 数量和上下文窗口

LLM 的上下文窗口按 Token 计数，不是按字符或单词。一个中文汉字通常约等于 1.5~2 个 Token，一个英文单词约等于 1.3 个 Token。上下文窗口 128K，意味着一次最多"看到"约 6 万~8 万汉字。

Token 数量直接影响 API 调用费用和推理速度。
